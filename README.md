#### Testing useContext with Enzyme

This project show an example testing useContext with Enzyme.

- Install dependencies: `npm install`
- Start project: `npm run start`
- Test project: `npm run test`
